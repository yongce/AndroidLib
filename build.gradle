buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.2.0'
    }
}

ext {
    compileSdkVersion = 22
    buildToolsVersion = "22.0.1"
}

allprojects {
    repositories {
        jcenter()
    }
}
